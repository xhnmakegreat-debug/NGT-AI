# ================================
# NGT-AI 决策系统 - 完整依赖文件
# Windows 11 优化版本
# ================================

# ============ 核心运行依赖 ============
# 注意：以下包是Python标准库，无需安装
# asyncio         # 异步I/O支持（内置）
# json           # JSON处理（内置）
# logging        # 日志系统（内置）  
# dataclasses    # 数据类支持（内置）
# typing         # 类型提示（内置）
# abc            # 抽象基类（内置）
# time           # 时间处理（内置）
# os             # 操作系统接口（内置）
# sys            # 系统特定参数（内置）
# pathlib        # 路径处理（内置）

# ============ 真实API集成依赖 ============
# OpenAI API
openai>=1.3.0

# Google Gemini API  
google-generativeai>=0.3.2

# Anthropic Claude API
anthropic>=0.8.0

# HTTP客户端（用于API调用）
httpx>=0.25.0
aiohttp>=3.9.0

# 数据验证和解析
pydantic>=2.5.0

# 配置文件处理
pyyaml>=6.0.1

# 异步文件I/O
aiofiles>=23.2.0

# ============ 数据处理和分析 ============
# JSON处理增强
orjson>=3.9.0

# 数据处理
pandas>=2.1.0
numpy>=1.24.0

# ============ Web界面依赖（可选） ============
# FastAPI Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 静态文件服务
aiofiles>=23.2.0

# 模板引擎
jinja2>=3.1.0

# WebSocket支持
websockets>=12.0

# ============ 开发和测试工具 ============
# 单元测试
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# 代码格式化
black>=23.9.0
isort>=5.12.0

# 代码检查
flake8>=6.1.0
mypy>=1.6.0

# 类型检查
types-PyYAML>=6.0.0
types-requests>=2.31.0

# ============ 日志和监控 ============
# 结构化日志
structlog>=23.2.0

# 进度条显示
tqdm>=4.66.0
rich>=13.6.0

# ============ 安全和认证 ============
# 环境变量管理
python-dotenv>=1.0.0

# 密钥加密
cryptography>=41.0.0

# ============ 数据库支持（可选） ============
# SQLite异步支持
aiosqlite>=0.19.0

# Redis支持
aioredis>=2.0.0

# ============ 文档生成 ============
# API文档
mkdocs>=1.5.0
mkdocs-material>=9.4.0

# ============ 部署和打包 ============
# 应用打包
pyinstaller>=6.0.0

# 依赖管理
pip-tools>=7.3.0

# ============ Windows特定依赖 ============
# Windows控制台颜色支持
colorama>=0.4.6

# Windows异步事件循环优化
winloop>=0.1.0; sys_platform == "win32"

# ============ 性能优化 ============
# C扩展的JSON解析器
ujson>=5.8.0

# 内存分析
memory-profiler>=0.61.0

# 并发限制
asyncio-throttle>=1.0.2

# ============ 错误监控 ============
# 错误追踪
sentry-sdk>=1.38.0

# ============ 缓存支持 ============
# 内存缓存
cachetools>=5.3.0

# ============ AI模型相关 ============
# Transformers（如果需要本地模型）
# transformers>=4.35.0
# torch>=2.1.0

# Langchain（如果需要）
# langchain>=0.0.340
# langchain-openai>=0.0.2

# ============ 开发工具增强 ============
# 自动重载
watchdog>=3.0.0

# 调试工具
ipdb>=0.13.0

# 性能分析
line-profiler>=4.1.0